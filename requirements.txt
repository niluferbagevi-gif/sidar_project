# ─────────────────────────────────────────────
# Sidar Project — Bağımlılıklar
# pip install -r requirements.txt
# ─────────────────────────────────────────────

# ─── Temel ───────────────────────────────────
python-dotenv>=1.0.0
requests>=2.31.0

# ─── AI Sağlayıcıları ────────────────────────
# Ollama (yerel LLM sunucusu — ayrıca kurulması gerekir)
# https://ollama.ai

# Google Gemini (opsiyonel)
google-generativeai>=0.7.0

# ─── Sistem İzleme ───────────────────────────
psutil>=5.9.0

# ─── GPU (opsiyonel — CUDA uyumlu sistem gerektirir) ──
# torch>=2.1.0+cu118  # GPU desteği için
# CPU için: torch>=2.1.0

# ─── GitHub Entegrasyonu ─────────────────────
PyGithub>=2.1.0

# ─── Geliştirme & Test ───────────────────────
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0
EOF